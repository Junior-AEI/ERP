{
    "devDependencies": {
        "husky": "^8.0.3"
    },
    "scripts": {
        "prepare": "husky install",
        "format:app": "cd app && npm run format ; cd ..",
        "format": "npm run format:app",
        "lint:app": "cd app && npm run lint ; cd ..",
        "lint:server": "cd server && npm run lint ; cd ..",
        "lint": "npm run lint:app && npm run lint:server",
        "dev:app": "cd app && npm run dev",
        "dev:server": "cd server && npm run dev",
        "dev": "npm run dev:app & npm run dev:server"
    },
    "dependencies": {
        "gitmoji-cli": "^9.1.0",
        "prettier": "^3.2.5"
    }
}
